# Setting up SQLite in Ubuntu

## 1. Download sqlite3
```bash
sudo apt update
sudo apt install sqlite3
sqlite3 --version
```

## 2. Download SQlite Browser

```bash
sudo apt install sqlitebrowser
sqlitebrowser
```